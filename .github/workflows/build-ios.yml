name: 🍏 Build Jellyboxd iOS (Unsigned IPA)

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-ios:
    name: 📦 Build Unsigned iOS IPA
    runs-on: macos-14
    env:
      DEVELOPER_DIR: /Applications/Xcode_16.2.app/Contents/Developer

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 🥟 Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: 🔧 Setup Xcode 16.2
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '16.2'

      - name: 🔧 Select Xcode 16.2 Toolchain & Verify iOS SDK
        run: |
          sudo xcode-select -switch /Applications/Xcode_16.2.app || sudo xcode-select -switch /Applications/Xcode_16.2.app/Contents/Developer
          xcodebuild -version
          swift --version
          SDK_PATH="$(xcrun --sdk iphoneos --show-sdk-path)"
          echo "iPhoneOS SDK found at: $SDK_PATH"
          test -d "$SDK_PATH"

      - name: 📦 Install Project Dependencies
        run: |
          bun install

      - name: ⚙️ Expo Prebuild (Generate iOS Native Project)
        run: |
          npx expo prebuild --platform ios --clean

      - name: 🧹 Clean Pods & Reinstall Dependencies with Xcode 16.2
        run: |
          rm -rf ios/Pods ios/Podfile.lock
          cd ios
          pod install --repo-update
          cd ..

      - name: 🛠️ Build iOS Archive
        env:
          DEVELOPER_DIR: /Applications/Xcode_16.2.app/Contents/Developer
        run: |
          xcodebuild \
            -workspace ios/Jellyboxd.xcworkspace \
            -scheme Jellyboxd \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath "$PWD/build/Jellyboxd.xcarchive" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            archive

      - name: 📦 Package Unsigned IPA
        run: |
          set -e
          APP_PATH=$(find build/Jellyboxd.xcarchive/Products/Applications -name "*.app" | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "❌ Error: Could not find .app in archive"
            exit 1
          fi
          echo "Found app at: $APP_PATH"
          mkdir -p build/Payload
          cp -R "$APP_PATH" build/Payload/
          cd build
          zip -r Jellyboxd.ipa Payload
          echo "✅ Created Jellyboxd.ipa successfully"

      - name: 📤 Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Jellyboxd-iOS-Unsigned-IPA
          path: build/Jellyboxd.ipa
          retention-days: 14

      - name: 🚀 Release on Tag
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v2
        with:
          files: build/Jellyboxd.ipa
          generate_release_notes: true
